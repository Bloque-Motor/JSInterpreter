function test5 (string name, int age, boolean isClient){
    if (!isClient) isClient = true;
    var string = 'underage'
    while (age < 18){
        print(name);
        print(u);
        age = age + string;
    }
}