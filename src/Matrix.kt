class Matrix {

    enum class States {
        P, B, B1, T, S, S1, S2, S3, S4, X, C, F, H, A, K, L, Q, E, R, U, U1, U2, V, V1
    }

    val terminales = listOf<>()
    val nterminales = States.values()
}